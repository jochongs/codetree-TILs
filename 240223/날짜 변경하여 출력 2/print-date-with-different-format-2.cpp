#include <iostream>
#include <string>
using namespace std;

int main() {
    // 여기에 코드를 작성해주세요.
    int yyyy, mm, dd;

    cin >> dd;
    cin.get();

    cin >> mm;
    cin.get();
    
    cin >> yyyy;
    cin.get();

    cout << yyyy << "." << mm << "." << dd;

    return 0;
}